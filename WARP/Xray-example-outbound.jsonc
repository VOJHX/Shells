{
  "outbounds": [
    {
      "tag": "WARP",
      "protocol": "freedom",
      "settings": {
        "domainStrategy": "UseIPv4v6" # 这里是 优先使用 IPv4 地址进行连接，你也可以对此进行修改。
      },
      "streamSettings": {
        "network": "raw",
        "sockopt": {
          "interface": "WARP" # 流量从 WARP 网口走
        }
      }
    }
  ]
}
